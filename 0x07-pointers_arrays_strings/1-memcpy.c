#include "main.h"
#include <stdlib.h>
/**
 * _memcpy - copies n bytes from memory
 * @n: copied bytes
 * @src: pointer to source mory
 * @dest: pointer to destination
 * Return: pointer to dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int m;

	for (m = 0; m < n; m++)

		dest[i] = src[i];
	return (dest);
}
