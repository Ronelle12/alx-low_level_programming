#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints the alhabet in small letters
 *
 * Return: 0
 */
int main(void)
{
	int n;

/* print from a - z */
	for (n = 'a'; n <= 'z'; n++)
		putchar(n);

	return (0);
}
